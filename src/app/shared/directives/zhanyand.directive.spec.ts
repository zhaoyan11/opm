import { ZhanyandDirective } from './zhanyand.directive';

describe('ZhanyandDirective', () => {
  it('should create an instance', () => {
    const directive = new ZhanyandDirective();
    expect(directive).toBeTruthy();
  });
});
